# HEAD

- Documented `OPENAI_BASE_URL` and `OPENAI_API_KEY` usage in README examples so
  `openai-python` can be configured through standard client environment
  variables, and clarified that `--api-key` is required for server-side
  incoming request authentication.
- Added Python 3.10 support and switched the validation matrix to Python 3.10.
